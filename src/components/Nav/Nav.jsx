import React from 'react';

export default function Nav() {
  return (
    <div style={container}>
    hi
    </div>
  );
}

const container = {
    height: '100%',
    width: '300px',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
}